<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    The local API is a Bun-compiled sidecar. Bun uses a JavaScriptCore JIT, so
    under Apple's hardened runtime (required for notarization) it is killed at
    launch without these entitlements. disable-library-validation lets the
    signed app load the sidecar signed with the same Developer ID.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
